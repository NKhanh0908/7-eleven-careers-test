spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/seveneleven}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:13524679}
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate.format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration

jwt:
  secret: ${JWT_SECRET:fDVidgXxPsgxmQnUPMECjPMLRJAeVZV9YuZpOoBo=}
  expiration-ms: 604800000
  cookie-name: access_token

server:
  port: 8080

cookie:
  secure: ${COOKIE_SECURE:false}
  same-site: Strict

cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:dri1gsb1d}
  api-key: ${CLOUDINARY_API_KEY:239694147792694}
  api-secret: ${CLOUDINARY_API_SECRET:VU3Cj_5IJRsM-2sr7cf6Ibzq3CQ}
  folder: ${CLOUDINARY_FOLDER:seven_eleven_product}